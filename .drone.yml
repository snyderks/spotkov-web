kind: pipeline
name: default

steps:
- name: build
  image: golang
  commands:
  - go get -u github.com/golang/dep/cmd/dep
  - go install github.com/golang/dep/cmd/dep
  - $GOPATH/bin/dep ensure
  - go build
  - go test -short
      
